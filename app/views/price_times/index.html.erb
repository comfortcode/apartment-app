<h1>Time (For Price) Dashboard</h1>

<table>
  <thead>
    <tr>
      <th>ID</th>
      <th>Name</th>
      <th>Notes</th>
      <th colspan="2"></th>
    </tr>
  </thead>
  <tbody>
      
    <% @price_times.each do |t|%>
      <tr>
        <td><%= t.id %></td>
        <td><%= t.name %></td>
        <td><%= t.notes %></td>
        <td><%#= link_to 'Edit', edit_currency_path(c) %></td>
        <td><%= link_to 'Destroy', t, method: :delete, data: { confirm: 'Are you sure you want to delete this time?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= form_for @new_price_time do |t| %>
   <%= t.text_field :name, placeholder: 'Name' %>
   <%= t.text_area :notes, placeholder: 'Notes' %>
   <%= t.submit "Save Rental Type" %>
 <% end %>